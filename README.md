# zigimg-test
Scripts to run different code snippets for zigimg repo.
Change the path of the 'zigimg' dependency directory to the path where you clone locally inside the build.zig.zon.
run `zig build run`
